[build]
  command = "npm run build"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["better-sqlite3"]
  included_files = ["data/movies.db"]

[functions."*"]
  node_bundler = "esbuild"

[build.environment]
  NODE_VERSION = "20"
